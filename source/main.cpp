#include <iostream>
#include <game.h>
#include <player.h>

// Game game;

int main ()
{
    // srand(NULL);

    // game.StartMenuScreen(game.debugging);

    while (1)
    {
        // game.Tick();

        // game.Update(game.debugging);
    }
    return 0;
}